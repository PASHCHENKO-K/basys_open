# Разовая проверка: доступны ли раннеры публичному репозиторию, когда
# минуты приватного исчерпаны. Публичным репозиториям стандартные раннеры
# даются бесплатно и в квоту не считаются — но если заблокирован счёт,
# а не только исчерпана квота, закрыт и этот путь.
#
# Файл временный и удаляется сразу после ответа.
name: проба
on:
  workflow_dispatch:
jobs:
  proba:
    runs-on: ubuntu-latest
    steps:
      - run: echo "раннер публичного репозитория доступен"
